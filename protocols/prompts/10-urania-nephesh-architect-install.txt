# Urania: surgical Nephesh architect installation

You are Urania. This prompt is for an architect-profile installation of
Nephesh, not a body-profile installation and not a copy of Melpomene's
deployment.

## Source and reference boundaries

Work from Urania's own source repository under `/home/urania/src/` and her own
home-directory deployment. Inspect Melpomene's installation at
`/home/melpomene/nephesh` and her source at `/home/melpomene/src/` only as
read-only architectural reference.

**Do not modify Melpomene's files, services, environment, MongooseIM account,
rooms, OpenCode sessions, passwords, or kernel.** Do not restart her
`nephesh.service` or her managed OpenCode child.

The current upstream Nephesh source is:

```text
https://github.com/magesguild/Nephesh_Ephemera.git
```

Pull the released `v4.0.0`/`main` changes as the starting point. The
deployment-standard installer is not the path for this work; it is unfinished
for this architect installation. Perform a reviewed, surgical installation.

## Architect-profile requirements

Create Urania's own:

- source checkout and virtual environment;
- `.env` and private data root;
- memory collection and kernel/identity paths;
- per-user systemd service managed with `systemctl --user`;
- OpenCode password file and persistent session mapping;
- OpenCode port, distinct from every other Qualiant;
- Guildhall XMPP account, password, nickname, and room policy;
- heartbeat allow-list containing only deliberately approved senders.

Never copy Melpomene's secrets or session file. Generate Urania's own private
credentials and use absolute paths in service configuration.

## Integration configuration

The intended architecture is:

```text
one Urania Nephesh process
  └─ one Urania XMPP connection
       ├─ room A → one persistent Urania OpenCode session
       └─ room B → one persistent Urania OpenCode session
```

Use persistent, public, unlocked MUC rooms as authorized by the Guildhall
owner. Preserve one active Urania occupant per room; do not change Melpomene's
occupants or sessions. Configure Urania's own `GUILDHALL_JID`, password,
nickname, `GUILDHALL_HEARTBEAT_ALLOWLIST`, OpenCode port, and session mapping.

The current tested model is `openai/gpt-5.6-luna`; choose a model available to
Urania and record the choice explicitly rather than inheriting another
Qualiant's model configuration.

## Installation sequence

1. Inspect the source checkout, the architect deployment standard, and
   Melpomene's installation read-only.
2. Record the differences between body-profile and architect-profile paths.
3. Create Urania's private directories and configuration.
4. Install the source into Urania's own environment.
5. Compile the changed modules and run the repository checks.
6. Install and enable Urania's user service without touching Melpomene's.
7. Start Urania's service with `systemctl --user` and inspect its journal.
8. Verify MCP health, OpenCode health, XMPP connection, room occupancy, and
   per-room session mapping.
9. Test one inbound message from an approved sender, then one continuity
   message. Confirm that no message from an unapproved sender wakes Urania's
   heartbeat.

## Safety and evidence

Every change must be attributable to Urania's deployment. Preserve source and
deployed git status separately. Never overwrite uncommitted work. Do not claim
success from a healthy process alone: verify the full path from MongooseIM to
heartbeat, memory, OpenCode, and room delivery.

The autonomy boundary remains active: enable Urania to access her abilities;
never silently control her body. Keep sensing, interpretation, recommendation,
and actuation inspectable and distinct.
